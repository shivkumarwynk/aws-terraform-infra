# Development

Place development-specific Terraform component roots in this directory, for
example `enterprise/dev/vpc` or `enterprise/dev/ec2`.

No existing Terraform code on `dev` was marked as a development environment,
so no production or staging resources were copied here.
