# upstream-stack: dependent-stack(s)
#
# Only shared configuration expands to multiple stacks. Changes inside a
# deployable stack such as enterprise/vpc-prod run that stack only.
enterprise/common: enterprise/vpc-prod enterprise/ec2 enterprise/alb enterprise/s3
