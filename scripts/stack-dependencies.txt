# upstream-stack: dependent-stack(s)
#
# When an upstream stack changes, location detection also runs each dependent.
# This keeps consumers that discover AWS resources by tags/names in sync.
enterprise/vpc-prod: enterprise/ec2 enterprise/alb
